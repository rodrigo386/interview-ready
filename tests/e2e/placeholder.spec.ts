import { test, expect } from "@playwright/test";

test("placeholder — real E2E tests added in Task 15 when Supabase is configured", async () => {
  // Sanity: verify Playwright setup. Replaced by real signup smoke in Task 15.
  expect(1 + 1).toBe(2);
});
